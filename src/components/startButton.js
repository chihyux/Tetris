import React from 'react'
import { StyledButton } from './styles/StyledButton'

const StartButton = () => {
    return (
        <StyledButton>
            Start Game
        </StyledButton>
    )
}

export default StartButton